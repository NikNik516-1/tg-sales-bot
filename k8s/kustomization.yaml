apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- 00-namespace.yaml
- 01-redis.yaml
- 02-rabbitmq.yaml
- 03-chromadb.yaml
- 10-listener.yaml
- 11-ai-worker.yaml
- 12-admin.yaml
# 20-ingress.yaml — подключить после установки cert-manager (Фаза 6.3)
# secret.yaml    — создать вручную из secret.template.yaml, НЕ в git
